{
  "mcpServers": {
    "truearch": {
      "command": "uvx",
      "args": ["truearch-mcp"],
      "description": "TrueArch — the hallucination killer for AI coding agents (local stdio, no clone). One-liner: claude mcp add truearch -- uvx truearch-mcp"
    }
  }
}
